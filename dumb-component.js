import React from 'react';
import PropTypes from 'prop-types';

const ${NAME} = (props) => {
  return (
    <div className="${NAME}">
      
    </div>
  );
};

${NAME}.propTypes = {

};

export default ${NAME};
